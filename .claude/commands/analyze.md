Analyze an audio file or audio-related code in the SampleMind project.

If given a file path to an audio file, describe what the audio analysis pipeline would do:
1. BASIC analysis: BPM, key, duration
2. STANDARD: + MFCC, chroma, spectral features
3. DETAILED: + harmonic/percussive separation
4. PROFESSIONAL: + AI classification, CLAP embeddings

If given a code path, review the audio processing implementation for correctness,
async patterns, sample rate handling, and memory efficiency.

Use $ARGUMENTS as the target file or module path.
