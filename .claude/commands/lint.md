Run Python linting and fix issues:

1. Run `ruff check src/ --fix` to auto-fix issues
2. Run `ruff format src/` to format code
3. Run `mypy src/` for type checking
4. Report any remaining issues that need manual attention
